datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

// Your placeholder testing model
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
